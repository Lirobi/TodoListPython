{
  "version": 2,
  "builds": [
    {
      "src": "TodoList/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "TodoList/wsgi.py"
    }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "TodoList.settings",
    "DEBUG": "false"
  },
  "buildCommand": "pip install -r requirements.txt && gunicorn TodoList.wsgi:application --bind 0.0.0.0:$PORT",
  "outputDirectory": "."
}
