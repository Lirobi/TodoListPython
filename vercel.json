{
  "version": 2,
  "builds": [
    {
      "src": "TodoList/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "TodoList/wsgi.py"
    }
  ],
  "env": {
    "DEBUG": "false"
  },
  "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic && gunicorn TodoList.wsgi:application --bind 0.0.0.0:$PORT",
  "outputDirectory": "."
}
